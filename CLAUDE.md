# Claude Code Instructions

Follow [AGENTS.md](AGENTS.md). In particular, preserve the independent implementation boundary, the model endpoint allowlist, opaque destination IDs, artifact-bound recursive scope, ambiguity-only per-run content consent, bounded local extraction, local-only fallback destinations, and the separate proposal, approval, planning, and apply boundaries.
