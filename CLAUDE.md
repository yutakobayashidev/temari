# Claude Code Instructions

Follow [AGENTS.md](AGENTS.md). In particular, preserve the independent implementation boundary, the model endpoint allowlist, opaque destination IDs, artifact-bound recursive scope, ambiguity-only per-run content consent, bounded local extraction, local-only fallback destinations, source-root writer locking, JSON authority for filesystem recovery, the separate proposal, approval, planning, and apply boundaries, `managed` as the sole public recurring workflow over the shared `temari-core::ManagedService`, workspace-bound model configuration, single-use backend-held desktop setup previews, explicit finite scheduling through `temari-schedule` without shell execution, durable file and directory move-back intent derived from authoritative journals, journaled root-directory adoption into `Kept`, and reprocessing through a reviewed Plan back to Inbox.
